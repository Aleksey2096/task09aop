Создать сервис по отправки сообщения. У него есть метод send(String message), который отвправляет
сообщение(Можно вывести на экран) нужно создать перехватчик этого сообщения(которыый будет тоже
вывовдить на экран "Сообщение перехваачено" + само сообщение) сделать через ProxyFactory
(как в презентации 25) сделать pull request

потом в новых двух  ветках для этого же сервиса  добавить метод sendException(который генерирует exception) и
сделать через аспекты(первый раз через xml конфигурацию, второй через анатации):

перед вызова метода send (вывести на экра "сейчас будет вызван метод send")

после вызова метода send (вывести на экра "Был вызван метод send")

вывести на экран сообщение message send

вывести на экран за сколько времени выполняется метод send

вывести на экран "Была сгенерированна ошибка", во время выполнения метода sendException
